type Props = { year: number; readOnly: boolean };

export function QuickAddPanel({ year: _year, readOnly: _readOnly }: Props) {
  return <div className="p-6 text-sm text-foreground/40">Quick add — coming soon</div>;
}
